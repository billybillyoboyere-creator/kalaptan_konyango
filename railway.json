{
  "$schema": "https://railway.com",
  "build": {
    "builder": "NIXPACKS",
    "rootStartCommand": "python backend/app.py"
  },
  "deploy": {
    "startCommand": "python app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
